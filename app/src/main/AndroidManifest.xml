<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JobPortal"
        tools:targetApi="31">
        <activity
            android:name=".SettingsFragement"
            android:exported="false"
            android:theme="@style/Theme.JobPortal" />
        <activity
            android:name=".JobPostingsDialog"
            android:exported="false"
            android:theme="@style/Theme.JobPortal" />
        <activity
            android:name=".DetailFragment"
            android:exported="false"
            android:theme="@style/Theme.JobPortal" />
        <activity
            android:name=".JobPostAdapter"
            android:exported="false"
            android:theme="@style/Theme.JobPortal" />
        <activity
            android:name=".JobPostListFragment"
            android:exported="true"
            android:enableOnBackInvokedCallback="true"

            android:theme="@style/Theme.JobPortal" />
        <activity
            android:name=".JobListingFragement"

            android:exported="false" />
        <activity
            android:name=".JobPosting"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.JobPortal" />
        <activity
            android:name=".Login"
            android:exported="true"
            android:theme="@style/Theme.JobPortal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Profile Edit Activity -->
        <activity
            android:name=".ProfileEdit"
            android:exported="false" />
    </application>

</manifest>